package org.example.DataStructures.Queue;

public class Queue {
    /**
     * <h1>Queue</h1>
     * <p>This is an interface of the Java collection framework and it provides the functionality of the queue data structure. It extends the collection framework.</p>
     * <p>
     *     <strong>In queues, elements are stored and accessed in First In,First Out manner.This means elements are added from the behind but removed from the front.</strong>
     * </p>
     * <h3>Classes that implement this interface:</h3>
     * <ul>
     *     <li>ArrayDeque</li>
     *     <li>LinkedList</li>
     *     <li>PriorityQueue</li>
     * </ul>
     * <br>
     * <h3>Methods of Queue:</h3>
     * <ul>
     *     <li>
     *         <strong>add()</strong>, inserts the specified element into the queue. If the task is successful, the methods returns true, if not an exception is thrown.
     *     </li>
     *     <li>
     *         <strong>offer()</strong>, inserts the specified element into the queue. If the task is successful, the method returns true if not it returns false.
     *     </li>
     *     <li>
     *         <strong>element()</strong>, returns the head od the queue, and throws and exception if the queue is empty.
     *     </li>
     *     <li>
     *         <strong>peek()</strong>, returns the head of the queue. It returns null if queue is empty.
     *     </li>
     *     <li>
     *         <strong>remove()</strong>, returns and removes the head of the queue. Throws an exception if queue is empty.
     *     </li>
     *     <li>
     *         <strong>poll()</strong>, returns and remove the head of the queue. Returns null if the queue is empty.
     *     </li>
     * </ul>
     */
    Queue(){}
}
